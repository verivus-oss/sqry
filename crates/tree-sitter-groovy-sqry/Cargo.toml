[package]
name = "tree-sitter-groovy-sqry"
version.workspace = true
edition.workspace = true
description = "Tree-sitter grammar for Groovy (sqry fork)"
license.workspace = true
repository.workspace = true
include = ["src/", "grammar-src/", "build.rs", "Cargo.toml"]

[dependencies]
tree-sitter.workspace = true
sqry-tree-sitter-support = { version = "16.0.6", path = "../../sqry-tree-sitter-support" }

[build-dependencies]
cc = "1"
