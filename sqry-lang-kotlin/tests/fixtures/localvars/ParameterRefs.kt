class ParameterRefs {
    fun greet(name: String): String {
        val greeting = "Hello, " + name
        return greeting
    }

    fun add(x: Int, y: Int): Int {
        val result = x + y
        return result
    }
}
